# Netlify Deploy Steps

## TODO
- [ ] Step 1: Run `netlify init` to link repo to Netlify (select 'Create & configure a new site', team, site name, deploy with Git integration? No, manual for static).
- [ ] Step 2: Configure build settings (pure static: root '.', publish dir '.', no build cmd).
- [ ] Step 3: Run `netlify deploy --prod --dir=.` to deploy.
- [ ] Step 4: Visit deployed URL.

## Completed

